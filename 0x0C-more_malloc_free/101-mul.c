#include "main.h"
#include <stdlib.h>
#include <stdio.h>

int look(char *str);
char *make_array(int size);
char *iterate_0s(char *str);
void get_prod(char *prod, char *mult, int digit, int zeroes);
void add_numbers(char *final_prod, char *next_prod, int next_len);
